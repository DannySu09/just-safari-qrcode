# Uncomment the next line to define a global platform for your project
platform :macos, '10.12'

target 'just-safari-qrcode' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for just-safari-qrcode

  target 'just-safari-qrcodeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'just-safari-qrcodeUITests' do
    # Pods for testing
  end

end

target 'just-safari-qrcode Extension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for just-safari-qrcode Extension

end

pod "QRCoder"
